const ShopSliderTop = (props) => {
    return (

        <div className="shopSlider__top">
            <h3 className="shopSlider__title">{props.title}</h3>
        </div>

    );
}

export default ShopSliderTop;